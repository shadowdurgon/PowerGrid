name: Bug Report
description: "Report some other kind of issue."
labels: [ "bug" ]
title: "[Bug] "
body:
  - type: textarea
    attributes:
      label: Description
      description: >
        Please describe the issue with as much detail as possible.
        Explain what happened, and what should have happened instead.
        Add images, screenshots, or videos if they could be useful.
    validations:
      required: true
  - type: dropdown
    id: version
    attributes:
      label: Version
      description: Select which mod loader and game version the bug occurs in
      options:
        - Forge 1.20.1
        - Fabric 1.20.1
        - NeoForge 1.21.1
      default: 0
    validations:
      required: true
  - type: input
    attributes:
      label: Game Log
      description: >
        We need the game log for additional information about the bug.
        This file can be found in the \"logs\" folder of your Minecraft folder as \"latest.log\".
        Make sure you turn on the `lotsOfLogs` config option before submitting your log.
        Please upload the file to https://mclo.gs/ and put the link here.
        Do **not** paste the *contents* of the file here, because that will make this issue very hard to read.
    validations:
      required: true

  - type: markdown
    attributes:
      value: "Thank you for taking the time to make a report and help improve Power Grid!"
